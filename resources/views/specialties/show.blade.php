<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            {{ __('Especialidade') }} - {{ $specialty->name ?? __('Sem Nome') }}
        </h2>
    </x-slot>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header"> {{ __('Medicos') }}</div>
                    <div card="card-body">
                        <ul>
                            @foreach ($specialty->medicos as $medico )
                                <li>{{ $medico->name }}</li>
                            @endforeach
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>
